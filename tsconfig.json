{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "paths": {
      "@/*": [
        "./*"
      ]
    }
  },
  // Include files (pages if you wish)
  "include": [
    "**/*.ts", // Inclui todos os arquivos ts
    "**/*.tsx", // Inclui todos os arquivos tsx
    "**/*.jsx", // Inclui todos os arquivos jsx
    ".expo/types/**/*.ts", //Entendeu né?
    "expo-env.d.ts",
    //"app/index.jsx" // Aqui não seria necessário 
    // colocar isso devido às inclusões acima
  ]
}